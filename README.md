# MYQuitMate – Flutter Prototype v1.1 (Clean)

This is a clean ZIP with proper folder structure.

## Quick Start
1. Unzip into `D:\flutter_projects\`
2. Open terminal:
   ```bash
   cd D:\flutter_projects\MYQuitMate_Flutter_Prototype_v1_1_clean
   flutter pub get
   flutter run -d chrome
   ```
